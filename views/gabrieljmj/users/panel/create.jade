extends ../../../layout

block content
    form(method="post")
        input#title(type="text", name="title", placeholder="Title")
        textarea#content(name="content", placeholder="content")
        input#create(type="submit", value="Create")
    if message
        span #{message}